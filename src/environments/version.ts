// Overwritten by deploy.yml at build time with the git release tag (or dev-<short-sha>
// for non-release builds), so the running app can report which build it actually is.
export const APP_VERSION = 'dev';
